version: '3'

services:
  nextjs:
    build:
      context: ./
      dockerfile: Dockerfile.dev
    depends_on:
      - backend
      - bff

  bff:
    build:
      context: ../consumption-bff
    ports:
      - 3005:3005

  backend:
    build:
      context: ../survey-service
    ports:
      - 8080:8080

  nginx:
    build: ./nginx
    depends_on:
      - nextjs
    ports:
      - 443:443
      - 80:80

