version: '3'

services:
  nextjs:
    build:
      context: ./
      dockerfile: Dockerfile.dev

  bff:
    build:
      context: ../consumption-bff
    ports:
      - 3005:3005

  backend:
    build:
      context: ../survey-service

  nginx:
    build: ./nginx
    ports:
      - 443:443
      - 80:80
