version: '3'

services:
  nextjs:
    build:
      context: ./
      dockerfile: Dockerfile.dev

  backend:
    build:
      context: ../survey-service

  nginx:
    build: ./nginx
    ports:
      - 443:443
      - 80:80
      - 8080:8080
