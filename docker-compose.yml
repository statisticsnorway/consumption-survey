version: '3'
services:
  react-ui:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "443:443"
    container_name: fbu-frontend
    volumes:
      - '.:/app'
      - '/app/node_modules'
    environment:
      - CHOKIDAR_USEPOLLING=true
