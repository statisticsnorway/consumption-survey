import LayoutWithFooter from '../components/layout/LayoutWithFooter';

const Consumption = () => {
    return (
        <LayoutWithFooter>
            <p>Consumption</p>
        </LayoutWithFooter>
    );
};

export default Consumption;
