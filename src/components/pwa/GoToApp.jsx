import { WorkspacePanel } from '../layout/Workspace';

const GoToApp = () => (
    <WorkspacePanel>
        <p>Looks like you have already installed the app!</p>
        <p>For best results please use the app instead of browser</p>
    </WorkspacePanel>
);

export default GoToApp;
