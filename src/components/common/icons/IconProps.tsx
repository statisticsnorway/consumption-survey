export type IconProps = {
    width?: number;
    height?: number;
    className?: string;
    style?: object;
};

export enum IconPosition {
    AFTER,
    BEFORE,
    TOP,
    BOTTOM,
    LEFT,
    RIGHT,
};
